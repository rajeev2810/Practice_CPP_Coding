class Sample
{

int wordfrequency(string& s)
{
sstringstream data;
FILE* f = fopen("sample.doc", r+);
data << f.readdata();
string s =  data.c_str();
std::map<string, int> numberofwords{};
	string sub;
	int count;
	string wordcount {};

while (f.eof != '/0')
{
size_t pos = s.find(' ');
	if (pos != s.end() && sub != encounteredstring)
{
	string encounteredstring;
	string sub = s.substr(s, pos);
	encounterstring[i] = sub;
	count++;
	i++;
	wordcount = encounteredstring[i] + itoa(count);
	

}	

}
wordcount.substr()

return wordcount;

    
}
}